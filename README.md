# OnaQ by shnq

Универсальная платформа для малого бизнеса: автоматизация заказов, CRM, витрина товаров/услуг, AI-консультант, аналитика.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js (версия 16 или выше)
- MongoDB (локально или удаленно)
- npm или yarn

### Установка и запуск

#### 1. Установка зависимостей

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

#### 2. Настройка переменных окружения

**Backend (.env):**
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/onaq
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

**Frontend (.env):**
```env
REACT_APP_API_URL=http://localhost:5000/api
```

#### 3. Запуск приложения

**Запуск Backend (в отдельном терминале):**
```bash
cd backend
npm run dev
# или
npm start
```

**Запуск Frontend (в отдельном терминале):**
```bash
cd frontend
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

Backend API будет доступен по адресу: http://localhost:5000

## 📁 Структура проекта

```
onaq/
├── backend/                 # Backend (Node.js + Express)
│   ├── models/             # MongoDB модели
│   ├── routes/             # API маршруты
│   ├── controllers/        # Контроллеры
│   ├── middleware/         # Middleware
│   └── server.js           # Главный файл сервера
│
├── frontend/               # Frontend (React + Tailwind CSS)
│   ├── public/             # Статические файлы
│   └── src/
│       ├── components/     # React компоненты
│       ├── pages/          # Страницы приложения
│       ├── utils/          # Утилиты
│       └── styles/         # Стили
│
└── README.md
```

## 🎯 Основные функции

### Лендинг-страница
- Описание платформы OnaQ
- Преимущества и возможности
- Визуальные карточки функций
- Анимации при скролле
- Кнопки призыва к действию

### Демо-витрина
- Поддержка разных типов бизнеса:
  - Салон красоты (услуги + запись)
  - Барбершоп (услуги + запись)
  - Цветочный магазин (товары + корзина)
  - Мини e-commerce (товары + корзина)
- Демонстрационные данные
- Интерактивные элементы

### AI-консультант (демо)
- Чат-бот с демо-ответами
- Подбор товаров/услуг
- Ответы на типовые вопросы

### Страницы
- `/` - Главная (лендинг)
- `/demo` - Демо-версия платформы
- `/features` - Описание функций
- `/pricing` - Тарифы
- `/feedback` - Форма обратной связи
- `/contact` - Форма заявки на подключение
- `/about` - О компании

### API Endpoints

**Backend API:**
- `GET /api/health` - Проверка здоровья API
- `POST /api/contacts` - Создание заявки на подключение
- `GET /api/contacts` - Получение всех заявок (admin)
- `POST /api/feedback` - Отправка обратной связи
- `GET /api/feedback` - Получение всех отзывов (admin)
- `GET /api/demo` - Получение всех типов бизнеса
- `GET /api/demo/:businessType` - Получение демо-данных по типу бизнеса

## 🛠 Технологии

### Frontend
- React 18
- React Router DOM 6
- Tailwind CSS 3
- Framer Motion (анимации)
- Axios (HTTP клиент)

### Backend
- Node.js
- Express.js
- MongoDB / Mongoose
- CORS
- Body Parser

## 📝 Особенности

- ✅ Минималистичный современный дизайн
- ✅ Адаптивная верстка (мобильные устройства)
- ✅ Анимации при скролле
- ✅ Hover-эффекты
- ✅ Валидация форм
- ✅ Демо-версия функционала
- ✅ Поддержка разных типов бизнеса
- ✅ AI-консультант (демо)

## 🚧 Развертывание

### Production Build

**Frontend:**
```bash
cd frontend
npm run build
```

Собранные файлы будут в папке `frontend/build`

### Переменные окружения для Production

Настройте переменные окружения для production среды:
- `NODE_ENV=production`
- `MONGODB_URI` - ваша MongoDB URI
- `FRONTEND_URL` - URL фронтенда
- `REACT_APP_API_URL` - URL backend API

## 📞 Контакты

Для подключения тарифа и консультации свяжитесь с командой shnq через форму на сайте.

## 📄 Лицензия

MIT
