# Быстрый старт OnaQ

## 🚀 Установка и запуск за 5 минут

### Шаг 1: Установка зависимостей

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

### Шаг 2: Настройка MongoDB

Убедитесь, что MongoDB запущен:
- Локально: `mongod`
- Или используйте MongoDB Atlas (облачный)

### Шаг 3: Настройка переменных окружения

**Backend** (создайте `backend/.env`):
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/onaq
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

**Frontend** (создайте `frontend/.env`):
```env
REACT_APP_API_URL=http://localhost:5000/api
```

### Шаг 4: Запуск

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm start
```

### Шаг 5: Откройте браузер

Перейдите по адресу: http://localhost:3000

## ✅ Готово!

Теперь вы можете:
- Просмотреть лендинг-страницу
- Попробовать демо-версию (`/demo`)
- Оставить заявку (`/contact`)
- Отправить отзыв (`/feedback`)

## 📝 Важные замечания

1. **MongoDB** должна быть запущена для работы форм обратной связи
2. **Backend** должен быть запущен перед frontend для работы API
3. Демо-данные загружаются с backend, поэтому оба сервера должны быть запущены

## 🔧 Troubleshooting

**Ошибка подключения к MongoDB:**
- Проверьте, что MongoDB запущен
- Проверьте MONGODB_URI в `.env`

**Ошибка CORS:**
- Убедитесь, что FRONTEND_URL в backend/.env правильный
- Проверьте, что backend запущен на порту 5000

**Ошибка API:**
- Проверьте, что REACT_APP_API_URL в frontend/.env правильный
- Убедитесь, что backend запущен

